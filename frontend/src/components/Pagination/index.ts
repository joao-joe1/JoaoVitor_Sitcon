import PaginationPaginate from "./PaginationPaginate";
import PaginationRootProvider from "./PaginationRoot";

export const Pagination = {
  RootProvider: PaginationRootProvider,
  Paginate: PaginationPaginate
}
